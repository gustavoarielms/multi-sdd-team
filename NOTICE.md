# Attribution and publication notice

## Original project

This repository derives from:

- Project: `multi-sdd-team`
- Repository: <https://github.com/ram4-dev/multi-sdd-team>
- Original package metadata name: `@rcarnicer/multi-team-sdd`
- Original package metadata author: `rcarnicer`

The fork retains this attribution and does not represent the original project as
work authored by the fork maintainer.

## Fork identity

The derivative package identity selected for this fork is:

```text
@gustavoarielms/sdd-codegraph-cli
```

`gustavoarielms` is the fork maintainer, not the author or owner of SDD,
CodeGraph, or the complete upstream implementation.

## SDD and CodeGraph

- SDD is referenced as a software development methodology and workflow concept.
  No ownership of SDD is claimed.
- CodeGraph is a separate external tool. It is not owned, maintained, or bundled
  by this fork.
- Any CodeGraph support in this fork is limited to configuration, initialization,
  synchronization, or interoperability guidance.

## Licensing status

At the time this notice was written, the upstream `package.json` declared the
license identifier `MIT`, but the upstream repository did not include a
standalone `LICENSE` file recognized by GitHub.

This fork therefore declares `"license": "UNLICENSED"`, keeps `"private": true`
in `package.json`, and must not be published to a package registry until the
upstream licensing notice or explicit distribution permission is confirmed and
the required license and copyright notices are included.

The upstream clarification request is tracked in
<https://github.com/ram4-dev/multi-sdd-team/issues/1>.
